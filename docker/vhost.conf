<VirtualHost *:80>
    ServerName localhost
    DocumentRoot /app/public
    <Directory /app/public>
        Options -Indexes
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>
