<form method="post" action="{{ path('assert_abstract_assert_delete', {'id': abstract_assert.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ abstract_assert.id) }}">
    <button class="btn">Delete</button>
</form>