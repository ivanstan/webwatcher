{% extends 'base.html.twig' %}

{% block title %}AbstractAssert{% endblock %}

{% block body %}
    <h1>AbstractAssert</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ abstract_assert.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('assert_abstract_assert_index') }}">back to list</a>

    <a href="{{ path('assert_abstract_assert_edit', {'id': abstract_assert.id}) }}">edit</a>

    {{ include('assert_abstract_assert/_delete_form.html.twig') }}
{% endblock %}