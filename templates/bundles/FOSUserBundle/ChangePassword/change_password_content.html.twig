<h1 class="title">Edit</h1>

{{ form_start(form, { 'action': path('fos_user_change_password'), 'attr': { 'class': 'mt-3' } }) }}
    {{ form_widget(form) }}
    <div>
        <button type="submit" class="btn btn-primary">Submit</button>
    </div>
{{ form_end(form) }}
