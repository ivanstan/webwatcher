<h1 class="title">Profile</h1>

{{ form_start(form, { 'action': path('fos_user_profile_edit'), 'attr': { 'class': 'mt-3' } }) }}
    {{ form_widget(form) }}
    <div>
        <button type="submit" class="btn btn-primary">Save</button>
    </div>
{{ form_end(form) }}
