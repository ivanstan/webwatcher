{% extends 'content.html.twig' %}

{% block title %}Edit Group | {{ parent() }}{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-10">
            <h1>Edit Group</h1>
        </div>
        <div class="col-2">
            {{ include('pages/groups/_delete_form.html.twig') }}
        </div>
    </div>

    {{ include('pages/groups/_form.html.twig') }}
{% endblock %}
