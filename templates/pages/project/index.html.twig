{% extends 'content.html.twig' %}

{% block title %}Projects | {{ parent() }}{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-10">
            <h1>Projects</h1>
        </div>
        <div class="col-2">
            <a data-toggle="tooltip"
               data-placement="left"
               title="Create new project"
               class="btn btn-primary mt-2 float-right"
               href="{{ path('project_new') }}">
                <i class="fa fa-plus-square"></i>
            </a>
        </div>
    </div>

    {{ breadcrumbs()|raw }}

    {% include 'components/flash-messages.html.twig' %}

    <table class="table table-striped">
        <thead>
            <tr>
                <th>Name</th>
                <th>Base URL</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for project in projects %}
            <tr>
                <td><a href="{{ path('project_show', {'project': project.id}) }}">{{ project.name }}</a></td>
                <td>{{ project.baseUrl }}</td>
                <td>
                    <a data-toggle="tooltip"
                       data-placement="bottom"
                       title="Edit project"
                       class="mr-1 ml-1"
                       href="{{ path('project_edit', {'project': project.id}) }}">
                        <i class="fa fa-edit"></i>
                    </a>
                    <a data-toggle="tooltip"
                       data-placement="bottom"
                       title="Visit project home page"
                       class="mr-1 ml-1"
                       target="_blank"
                       href="{{ project.baseUrl }}">
                        <i class="fa fa-external-link-alt"></i>
                    </a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3" class="text-center">Create a project for starters.</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
