{% extends 'content.html.twig' %}

{% block title %}Edit Page {{ resource.name }}| {{ parent() }}{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-10">
            <h1>Edit Resource</h1>
        </div>
        <div class="col-2">
            {{ include('pages/resource/_delete_form.html.twig') }}
        </div>
    </div>

    {{ breadcrumbs(resource)|raw }}

    {% include 'components/flash-messages.html.twig' %}

    {{ include('pages/resource/_form.html.twig', {'button_label': 'Update'}) }}
{% endblock %}
