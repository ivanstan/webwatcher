{% extends 'base.html.twig' %}

{% block title %}TestAction{% endblock %}

{% block body %}
    <h1>TestAction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ test_action.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('action_test_action_index') }}">back to list</a>

    <a href="{{ path('action_test_action_edit', {'id': test_action.id}) }}">edit</a>

    {{ include('action_test_action/_delete_form.html.twig') }}
{% endblock %}